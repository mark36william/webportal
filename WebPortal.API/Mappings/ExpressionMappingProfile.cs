using AutoMapper;

namespace WebPortal.API.Mappings;

public class ExpressionMappingProfile : Profile
{
    public ExpressionMappingProfile()
    {
        // This profile is used to enable expression mapping
        // All other mappings should be in the main MappingProfile
    }
}
